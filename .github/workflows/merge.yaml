name: merge
on:
  push:
    branches: [ main ]
#   pull_request:
#     types: [closed]
#     branches: 
#       - main

jobs:
  test:
    runs-on: ubuntu-18.04
    #if: github.event.pull_request.merged == true   
   
#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v2
#         with:
#           fetch-depth: 2
#           FILES: |
#             index.html
      
      - uses: technote-space/get-diff-action@v6
        with:
          # PATTERNS: |
          #   +(src|__tests__)/**/*.ts
          #   !src/exclude.ts
          FILES: |
            index.html
            
#       - name: slack notificator
#         uses: brainly/action-slack-notificator@1.0.2
#         with:
#          webhookUrl: ${{ secrets.SEND_SLACK_NOTIFICATION }}
#          messageContent: View the test results on Github

      - name: "Check if should send slack notification"
        id: send-slack-notification
        uses: peter-murray/value-as-flag-action@0.0.1
        with:
          value: true
           #default: false
          
    

      - name: "Notify Slack"
        if: steps.send-slack-notification.outputs.value == 'true'
        run: |
          curl -X POST -H 'Content-type: application/json' --data '{"text":" View hello the test results on Github: https://github.com/0chain/system_test/actions/runs/${{ github.run_id }}"}' ${{ secrets.CHANNEL_WEBHOOK_URL }}
